[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "artiecli"
readme = "README.md"
version = "0.0.1"
requires-python = ">=3.10,<3.12"
license = { text = "MIT" }
dependencies = [
    "rpyc==5.3.1"
]

[project.scripts]
artie-cli = "artiecli.cli:main"
