# Artie Common

This folder contains a bunch of common components used between different versions of Artie,
but which do not belong in the framework (i.e., they do not necessarily belong to *all*
versions of Artie, but they are used by more than one).
