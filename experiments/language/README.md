# Language

This folder contains code corresponding to language experiments. Common code that might be used
by any experiment is found in this directory, along with main.py, which runs a language experiment,
and the experiments folder, which contains code that is not widely useful except to specific experiments.
