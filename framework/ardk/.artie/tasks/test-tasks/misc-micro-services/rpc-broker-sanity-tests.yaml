name: service-broker-sanity-tests
labels:
  - docker-image
  - sanity
dependencies:
  - artie-service-broker: docker-image
type: test
steps:
  - job: single-container-sanity-suite
    steps:
      - test-name: start-service-broker
        docker-image-under-test:
          dependency:
            name: docker-image
            producing-task: artie-service-broker
        cmd-to-run-in-dut: "python -m src.main --loglevel info --help"
