name: eyebrows-driver-hardware-tests
labels:
  - docker-image
  - hardware
dependencies:
type: test
steps:
  - job: hardware-test-suite
    steps:
      - test-name: hw-eyebrows
        cmds-to-run-in-cli:
          - "artie-cli eyebrows self-test"
          - "artie-cli eyebrows status"
        expected-results:
          - "TODO"
